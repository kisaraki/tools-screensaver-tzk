# tools-screensaver-tzk v0.10.2

這個版本新增 Setup 的產品版本衝突處理：

- 偵測到相同版本時允許修復安裝，沿用固定 AppId，不建立第二個解除安裝項目。
- 偵測到較舊或較新版本時，顯示兩個版本並詢問是否先移除既有版本再安裝本版。
- 使用者拒絕、找不到 uninstaller、解除安裝失敗、舊解除安裝項仍存在或靜默安裝遇到版本衝突時停止安裝，避免兩版並存。
- 解除舊版時保留個人模式與外觀偏好。

成品未簽章。實際升級／降版／同版修復、UAC、多帳號、檔案使用中及 Windows 11 尚未驗證。
